#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main(){
	ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
	
	int t;
	cin >> t;
	
	while(t--){
		int n;
		cin >> n;
		
		int left = 1, right = n/2;
		while(left <= right){
			int mid = (left+right) >> 1;
			
			
		}
	}
	
    return 0;
}

