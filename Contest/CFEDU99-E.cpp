#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main(){
	ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
	
	int t;
	cin >> t;
	
	while(t--){
		pair<int, int> p[4];
		for(int x=0;x<4;x++){
			cin >> p[x].first >> p[x].second;
		}
		
		for(int x=0;x<4;x++){
			for(int y=0;y<4;y++){
				int posX = p[x].first, posY = p[y].second;
				
				
			}
		}
	}
	
    return 0;
}

