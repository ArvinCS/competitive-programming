#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;

#define ll long long

int main(int argc, char * argv[]){
	registerValidation(argc, argv);
	
	int X = inf.readInt(1, 1e4, "X"); inf.readSpace();
	int Y = inf.readInt(1, 1e4, "Y");
	
	inf.readEoln();
	inf.readEof(); 
}


